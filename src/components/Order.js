import React from "react";

const Order = props => {
  console.log(props.order);
  // let { title, categories, price } = props.order;
  return (
    <div className="order-row">
      <h1>order</h1>
    </div>
  );
};
export default Order;
